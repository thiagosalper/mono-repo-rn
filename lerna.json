{
  "npmClient": "yarn",
  "useWorkspaces": true,
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "command": {
    "publish": {
      "registry": "http://localhost:4873"
    },
    "version": {
      "allowBranch": ["master"],
      "message": "chore(release): publicando versao",
      "noCommitHooks": true,
      "ignoreChanges": ["yarn.lock"],
      "conventionalCommits": true
    }
  }
}
